# Experiment Tracking

## EXP 1.1.1

In this experiment, we try various gap sizes and various places in the dataset where we put the gap. There was a bug so I discarded the experiment.

## EXP 1.1.2

This experiment fixes the bug from exp1.1.1 where samples for x=100.0 were added to the dataset when the end_x=100.0. It also adds 